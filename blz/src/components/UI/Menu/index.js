 {/*import React, {useState} from 'react';
import styles from "./Menu.module.scss"

const Menu = ({header, itmes}) => {
    return (
        <div className={styles.menu_content}>
        <div className={styles.menu_header}>{header}</div>
        <ul>
            {itmes.map(item =>
                <li>
                    <a href={item.href}>{item.value}</a>
                </li>
            )}
        </ul>
        </div>
    );
};

export default Menu;*/}